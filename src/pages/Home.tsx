import React from 'react';
import HeroSlide from '../component/hero-slide/HeroSlide';

const Home = () => {
    return (
        <div>
            <HeroSlide />
        </div>
    );
};

export default Home;
